# Temporal — defaults match the orchestration compose stack
TEMPORAL_ADDRESS=localhost:7234
TEMPORAL_NAMESPACE=default
TASK_QUEUE=gpu-workloads

# Kubernetes — path to kubeconfig for the real GPU cluster (set when you have one)
# KUBECONFIG=./kubeconfig

# Optional: default namespace for Volcano jobs
GPU_NAMESPACE=gpu-lab
